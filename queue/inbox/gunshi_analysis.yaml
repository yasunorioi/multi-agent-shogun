# queue/inbox/gunshi_analysis.yaml
# 軍師（gunshi）の分析出力。家老が読む一方向通信。
# ─────────────────────────────────────────────────────────────
# rotation-planner 委託業務モデル設計
# ─────────────────────────────────────────────────────────────
task_id: null
cmd_id: null
timestamp: "2026-03-22T19:28:01"

analysis:

  # ─── Bloom ルーティング ───────────────────────────────────
  bloom_level: 5
  bloom_reasoning: |
    「本番稼働中のDBスキーマに地主/受託者の概念を非破壊的に導入し、
    出荷管理システムとの将来統合を見据えた段階的設計案を策定」
    = Bloom L5（評価）。既存スキーマとの整合性評価、4案のトレードオフ比較、
    3段階移行計画の策定が中心。
    L6ではない理由: 既存テーブル構造へのインクリメンタルな拡張設計であり、
    ゼロからのアーキテクチャ創造ではない。
    L4ではない理由: 複数案の比較評価と段階的移行判断を含む。
  recommended_model: claude-sonnet-4-6
  confidence: 0.82

  # ─── 品質基準 ───────────────────────────────────────────
  quality_criteria:
    - "既存19テーブルのうち影響を受けるテーブルが明示されていること"
    - "fields.user_idの意味が変わらないこと（既存API後方互換）"
    - "field_contracts テーブルのDDLが完全であること"
    - "委託スコープ（full/partial）の表現方法が具体的であること"
    - "年度変更への対応が設計されていること"
    - "出荷管理システムとの暫定→正式の移行パスがあること"
    - "Phase 0→1→2の各段階が独立して動作すること"
    - "マイグレーションSQLが非破壊的であること（ALTER TABLE ADD COLUMNのみ）"
  qc_method: lord_review

  # ─── PDCA判定 ───────────────────────────────────────────
  pdca_needed: false

  # ─── North Star整合 ─────────────────────────────────────
  north_star_alignment:
    status: aligned
    reason: |
      「農業の現実を写し取れ。地主と作業者は別の人間だ」に直結。
      現行の fields.user_id が暗黙に「地主=作業者」を前提にしている問題を
      owner_id + field_contracts で解決。
      既存データ・APIを壊さない段階的設計により、本番稼働中のシステムを守る。
    risks_to_north_star:
      - "見落とし4番: 同一ほ場に複数受託者がいるケースの UNIQUE制約問題"
      - "地主がアプリに未登録の場合、owner_idが設定できない"
      - "防除記録の帰属問題（受託者が散布→地主の帳簿に載せる必要）が未解決"

  # ─── 予測成果物（Foreman方式） ─────────────────────────
  predicted_outcome:
    expected_files:
      - path: "docs/rotation_planner/contract_design.md"
        change_type: "new"
        description: "委託業務モデル設計書（6章構成）"
    expected_tests: |
      テストなし（設計書成果物）。
      実装Phase 1-2で個別テスト作成。
    expected_behavior: |
      設計書を元に、殿が以下を判断:
      1. Phase 0の出荷管理暫定運用の方針承認
      2. Phase 1のowner_id追加の実装開始タイミング
      3. 見落とし4番（複数受託者）の対処方針
    verification_method: |
      殿のレビューによる承認。特に:
      - UNIQUE制約の修正要否（見落とし4番）
      - 地主未登録時の対応方針
      - 防除記録の帰属ルール

  # ─── 戦略分析 ────────────────────────────────────────────
  strategy_analysis:
    alternatives:
      - name: "案A: 3段階移行（Phase 0→1→2）【推奨】"
        pros:
          - "非破壊的。各Phase独立して動作"
          - "既存API後方互換。fields.user_idの意味を変えない"
          - "Phase 0で出荷管理が先行開発可能"
          - "撤退容易（Phase 1,2はいつでもやめられる）"
        cons:
          - "Wave 3まで全機能揃わない"
          - "owner_id（Phase 1）とfield_contracts（Phase 2）の二段階が冗長に見える可能性"
        score: 9
      - name: "案B: users.role拡張"
        pros:
          - "スキーマ変更最小"
        cons:
          - "地主兼受託者に対応不可（排他ロール前提）"
          - "can_access_all_dataプロパティが壊れる"
        score: 3
      - name: "案C: 完全分離（owners+operators別テーブル）"
        pros:
          - "正規化が完全"
        cons:
          - "19テーブル+全クエリに波及"
          - "本番稼働中のシステムに致命的"
        score: 4
      - name: "案D: VIEWで仮想解決（冒険的）"
        pros:
          - "既存テーブル変更ゼロ"
          - "Phase 1スキップの可能性"
        cons:
          - "VIEWは書き込み不可"
          - "パフォーマンス未検証"
        score: 6
    recommendation: "案A: 3段階移行（Phase 0→1→2）"
    recommendation_reason: |
      本番稼働中のrotation-plannerに対して、非破壊的かつ段階的に
      委託概念を導入する最も安全な方法。
      出荷管理システムが別DB・別アプリとして先行開発できるため、
      rotation-planner側の拡張を急ぐ必要がない。
      Phase 1の実装コストは1-2 subtask（ALTER TABLE + API追加のみ）で軽い。

skill_candidate: null
