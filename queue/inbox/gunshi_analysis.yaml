# queue/inbox/gunshi_analysis.yaml
# 軍師（gunshi）の分析出力。家老が読む一方向通信。
# ─────────────────────────────────────────────────────────────
# ベクトル検索（sqlite-vec + Ruri v3）導入設計
# ─────────────────────────────────────────────────────────────
task_id: null
cmd_id: null
timestamp: "2026-03-24T01:44:40"

analysis:

  # ─── Bloom ルーティング ───────────────────────────────────
  bloom_level: 5
  bloom_reasoning: |
    「既存FTS5にsqlite-vecベクトル検索を追加するハイブリッド検索の設計。
    sui-memoryの設計パターンを没日録に適用し、モデル選定・RRF統合・
    時間減衰・VPS制約を評価」
    = Bloom L5（評価）。既存技術の適用評価、4モデルの比較、
    コスト・パフォーマンストレードオフ分析が中心。
    L6ではない理由: sui-memoryの確立された設計パターンの適用であり、
    ゼロからのアーキテクチャ創造ではない。
  recommended_model: claude-sonnet-4-6
  confidence: 0.90

  # ─── 品質基準 ───────────────────────────────────────────
  quality_criteria:
    - "sqlite-vec + vec_index仮想テーブルが没日録DBに追加されること"
    - "既存FTS5検索（search_index）が壊れていないこと"
    - "migrate_vec.pyで既存2,345件のバッチベクトル化が完了すること"
    - "search --hybrid でRRF統合結果が返ること"
    - "--hybrid なしでは従来のFTS5検索が動作すること（後方互換）"
    - "sqlite-vec未インストール環境でもCLIがエラーなく動作すること"
    - "検索レイテンシが200ms以下であること"
    - "モデル遅延ロード: 非検索コマンド実行時にモデルがロードされないこと"
  qc_method: ohariko

  # ─── PDCA判定 ───────────────────────────────────────────
  pdca_needed: false
  pdca_reasoning: |
    sui-memoryで確立された設計パターンの適用。
    Phase 0の実装自体はリスク低（既存を壊さない追加のみ）。
    ただしPhase 2の時間減衰パラメータ調整はベンチマーク結果に依存するため、
    Phase 2着手時にPDCA再評価を推奨。

  # ─── North Star整合 ─────────────────────────────────────
  north_star_alignment:
    status: aligned
    reason: |
      「意味検索で没日録の知見を掘り起こせ。FTS5を壊すな、拡張せよ」に直結。
      FTS5はキーワード一致のみ。ベクトル検索で意味的に近い知見を発掘。
      マクガイバー精神: SQLite + Python、外部依存2つのみ、月額ゼロ。
      sui-memoryの実績（7,059チャンク、100ms、1,759行Python）が有効性を実証。
    risks_to_north_star:
      - "VPSメモリ不足（457MB記録 vs 4GB記載）でagent-swarm展開困難"
      - "遅延ロードの初回10秒待ちがUXを損なう可能性"
      - "MeCab分かち書きテキスト vs rawテキストのベクトル化精度問題"
      - "モデルバージョン更新時の全ベクトル再計算コスト"

  # ─── 予測成果物（Foreman方式） ─────────────────────────
  predicted_outcome:
    expected_files:
      - path: "docs/shogun/vector_search_design.md"
        change_type: "new"
        description: "ベクトル検索導入設計書（7章+付録3本）"
      - path: "scripts/botsu/vec.py"
        change_type: "new"
        description: "共通ベクトル検索モジュール（VecSearchクラス）"
      - path: "scripts/migrate_vec.py"
        change_type: "new"
        description: "既存FTS5データのバッチベクトル化スクリプト"
      - path: "scripts/botsunichiroku.py"
        change_type: "modify"
        description: "search --hybrid オプション追加"
      - path: "scripts/botsu/__init__.py"
        change_type: "modify"
        description: "vec_upsert関数追加（インクリメンタル更新）"
    expected_tests: |
      - search --hybrid でRRF結果が返る
      - search（hybridなし）でFTS5のみ結果が返る（後方互換）
      - sqlite-vec未インストールでCLIエラーなし
      - バッチベクトル化: 2,345件が4分以内に完了
      - 検索レイテンシ: 200ms以下
    expected_behavior: |
      Phase 0完了時:
      - `search "農家が困っていること" --hybrid` で意味検索が動作
      - キーワード不一致でも意味的に近い知見がヒット
      - 既存FTS5検索は従来通り動作
      - モデルは検索コマンド実行時のみロード（他コマンドに影響なし）
    verification_method: |
      1. migrate_vec.py実行 → vec_indexに2,345件登録確認
      2. search --hybrid "テストクエリ" → 結果件数・関連性確認
      3. search "テストクエリ"（hybridなし）→ 従来結果と同一
      4. pip uninstall sqlite-vec → CLIが正常動作

  # ─── 戦略分析 ────────────────────────────────────────────
  strategy_analysis:
    alternatives:
      - name: "案A: sqlite-vec + Ruri v3-310m（ローカル推論）【推奨】"
        pros:
          - "月額ゼロ（ローカル推論のみ）"
          - "日本語特化モデルで没日録に最適"
          - "SQLite 1ファイル原則を維持"
          - "sui-memoryで実績あり"
          - "外部依存2つのみ"
        cons:
          - "初回モデルロード~10秒"
          - "VPS展開時のメモリ制約"
          - "PyTorch依存（ディスク~2GB）"
        score: 9
      - name: "案B: OpenAI Embeddings API"
        pros:
          - "ローカルモデル不要。メモリ節約"
          - "高品質ベクトル"
        cons:
          - "月額課金（殿の忌避事項）"
          - "オフライン動作不可"
          - "プライバシーリスク（データ外部送信）"
        score: 2
      - name: "案C: FTS5のみ（ベクトル検索なし）"
        pros:
          - "追加依存ゼロ"
          - "実装コストゼロ"
        cons:
          - "意味検索不可"
          - "キーワード不一致の知見を掘り起こせない"
          - "North Starに到達しない"
        score: 4
      - name: "案D: ChromaDB / Qdrant等の専用ベクトルDB"
        pros:
          - "HNSW等の高速ANN"
          - "豊富なフィルタリング機能"
        cons:
          - "別プロセス・別ファイル（SQLite 1本原則に違反）"
          - "Dockerまたはサーバー運用が必要"
          - "sui-memoryの前身(claude-mem)がChromaDBで失敗した教訓"
        score: 3
    recommendation: "案A: sqlite-vec + Ruri v3-310m（ローカル推論）"
    recommendation_reason: |
      殿の制約（月額忌避・SQLite・マクガイバー精神）に完全一致。
      sui-memoryが同じ構成で7,059チャンク・100ms検索を達成しており、
      没日録の2,345件では余裕で動作する。
      ChromaDB(案D)はsui-memoryの前身(claude-mem)が3日で失敗した教訓がある。
      SQLite 1ファイルに全てを収める設計が最もロバスト。

skill_candidate: null
