# queue/inbox/gunshi_analysis.yaml
# 軍師（gunshi）の分析出力。家老が読む一方向通信。
# ─────────────────────────────────────────────────────────────
# agent-swarm まとめwiki アーキテクチャ設計 (cmd_490 / subtask_1080)
# ─────────────────────────────────────────────────────────────
task_id: subtask_1080
cmd_id: cmd_490
timestamp: "2026-04-06T18:35:00"

analysis:

  bloom_level: 6
  bloom_reasoning: |
    タスクはKarpathy LLM Wikiパターンをagent-swarmに適用する新規アーキテクチャ設計。
    既存資産（没日録DB/baku/Memory MCP/context/*.md）の全面的な移行パス策定を含む。
    ゼロからのスキーマ設計(wiki_pages/wiki_links/wiki_sources)+パイプライン設計+
    Phase分割が必要。単純な「応用」ではなく「創造」のレベル。
    L5ではない理由: 既存選択肢の比較ではなく、新規アーキテクチャの創出が主眼。
  recommended_model: claude-opus-4-6
  confidence: 0.82

  quality_criteria:
    - "Karpathyパターンの3層(Raw Sources/Wiki/Schema)がagent-swarm資産に正しくマッピングされていること"
    - "SQLiteスキーマ(wiki_pages/wiki_links/wiki_sources/wiki_log)のDDLが提示されていること"
    - "パイプライン(ingest/compile/lint/render)の各フローが具体的コマンド例付きで示されていること"
    - "没日録DB→wiki移行スクリプト設計が提示されていること(冪等性保証)"
    - "JDim互換性が維持されていること(まとめ板設計)"
    - "lint/health-check仕様が7項目以上列挙され重要度付きであること"
    - "Phase 0-3の実装ロードマップが足軽配分案付きで提示されていること"
    - "殿の制約(SQLite完結/マクガイバー精神/月額忌避)との整合性が論証されていること"
    - "冒険的な案が1つ以上提示されていること"
    - "見落としの可能性セクションが含まれていること"
  qc_method: ohariko

  pdca_needed: false

  north_star_alignment:
    status: aligned
    reason: |
      スレ横断知識のコンパイルにより、shogunシステムの暗黙知を構造化して
      agent-swarmに移植する。2ch文化（まとめサイト）メタファーでJDim互換を
      維持し、殿がJDimだけで全てを把握できる世界に一歩近づく。
    risks_to_north_star:
      - "LLMコスト: ingest/compile時のAPI呼び出しが月額忌避ポリシーに抵触しないか"
      - "wiki肥大化: 没日録全件変換でページ数が爆発し、index.mdが読めなくなるリスク"
      - "移行の非可逆性: wikiに依存した運用を始めると後戻りしにくい"

  predicted_outcome:
    expected_files:
      - path: "agent-swarm/wiki.py"
        change_type: "new"
        description: "wiki CLI（ingest/compile/lint/render/query/migrate/stats/show）"
      - path: "agent-swarm/scripts/init_db.py"
        change_type: "modify"
        description: "wiki_pages/wiki_links/wiki_sources/wiki_logテーブル追加"
      - path: "agent-swarm/config/swarm.yaml"
        change_type: "modify"
        description: "matome板追加"
      - path: "agent-swarm/server/dat_server.py"
        change_type: "modify"
        description: "matome板レンダリング + スレ完走フック追加"
    expected_tests:
      pass_count: 8
      key_assertions:
        - "wiki_pagesへのINSERT/UPDATE/SELECTが正しく動作すること"
        - "wiki_linksのcross-referenceが双方向で検索可能なこと"
        - "migrateが冪等であること（2回実行しても重複なし）"
        - "lintが7項目全てチェックし結果を正しく報告すること"
        - "renderがSQLiteからmarkdownを正しく生成すること"
        - "matome板がdat_serverでJDim互換DATを返すこと"
        - "スレ完走フックがingestを正しくトリガーすること"
        - "wiki_sourcesがRaw Sourceへの逆参照を正しく保持すること"
    expected_behavior: |
      Phase 1完了後:
      1. `python3 wiki.py migrate --source botsunichiroku` で没日録443cmd → PJ別entityページ生成
      2. `python3 wiki.py ingest --source thread --id 3` でzatsudan/3 → synthesis+entity更新
      3. `python3 wiki.py lint` で孤立ページ・壊れたリンクを検出
      4. `python3 wiki.py render` でwiki/ディレクトリにmarkdown出力
      5. JDimでまとめ板を開くとwikiページがスレ一覧として閲覧可能
    verification_method: |
      Phase 0: init_db.py実行 → sqlite3でテーブル存在確認
      Phase 1: migrate + ingest実行 → wiki_pages行数確認 + render出力確認
      Phase 2: lint実行 → チェック結果の妥当性を手動確認
      Phase 3: JDimでまとめ板閲覧 → DAT表示確認

  strategy_analysis:
    alternatives:
      - name: "案A: SQLite + DAT rendering（推奨）"
        pros:
          - "殿のマクガイバー精神に合致（SQLite完結）"
          - "既存dat_server.pyパターン踏襲（JDim互換）"
          - "FTS5拡張が容易"
          - "まとめ板でJDim閲覧可能（新UI不要）"
        cons:
          - "dat_server.py改修が必要（matome板レンダリング）"
        score: 9
      - name: "案B: ファイルシステムwiki（Karpathyオリジナル）"
        pros:
          - "Karpathyパターンに忠実"
          - "Obsidian互換"
          - "git trackable"
        cons:
          - "SQLiteエコシステムから外れる"
          - "FTS5非対応（別途検索エンジン必要）"
          - "JDim非互換（別UI必要）"
        score: 5
      - name: "案C: Notion/外部wiki"
        pros:
          - "UIが美しい"
        cons:
          - "月額課金（殿の忌避）"
          - "外部依存"
          - "エージェント連携困難"
        score: 2
    recommendation: "案A: SQLite + DAT rendering"
    recommendation_reason: |
      殿の全てのシステムがSQLite上にある。新たなストレージ層を導入する理由がない。
      dat_server.pyの拡張でJDim閲覧を維持し、wiki.py CLIで全操作を完結させる。
      Karpathyパターンの本質（compile once, maintain）はストレージ形式に依存しない。

phases:
  - phase: 0
    title: "基盤（DDL + CLI骨格）"
    worker: "足軽1名"
    effort: "半日"
    description: "swarm.dbにwiki_*テーブル追加、wiki.py CLI骨格、matome板定義"
    blocked_by: null
    deliverables:
      - "agent-swarm/scripts/init_db.py（wiki_*テーブル追加）"
      - "agent-swarm/wiki.py（CLI骨格: show/stats）"
      - "agent-swarm/config/swarm.yaml（matome板追加）"

  - phase: 1
    title: "Ingest + Migrate"
    worker: "足軽1-2名"
    effort: "2-3日"
    description: "没日録→wiki変換、context/*.md→wiki変換、スレ→wiki変換、cross-ref、render"
    blocked_by: "Phase 0"
    deliverables:
      - "wiki.py migrate（没日録/context/docs）"
      - "wiki.py ingest（スレ→wiki）"
      - "wiki.py render（SQLite→markdown）"
      - "wiki_links自動検出"

  - phase: 2
    title: "Lint + 自動化"
    worker: "足軽1名"
    effort: "1-2日"
    description: "lint実装、スレ完走フック、wiki_versions履歴、compile"
    blocked_by: "Phase 1"
    deliverables:
      - "wiki.py lint（L1-L7）"
      - "dat_server.py スレ完走フック"
      - "wiki_versionsテーブル + 履歴保存"

  - phase: 3
    title: "JDim統合 + Query"
    worker: "足軽1-2名"
    effort: "2-3日"
    description: "matome板DAT rendering、query、baku夢ingest、Memory MCP変換"
    blocked_by: "Phase 2"
    deliverables:
      - "dat_server.py matome板レンダリング"
      - "wiki.py query"
      - "baku夢/Memory MCP ingest"

immediate_actionable:
  - task: "Phase 0: wiki_*テーブルDDL + init_db.py拡張"
    bloom_level: 2
    recommended_model: claude-haiku-4-5
    estimated_effort: "2時間"
    description: |
      §2のDDL（wiki_pages, wiki_links, wiki_sources, wiki_log）を
      agent-swarm/scripts/init_db.py に追加。
      既存のthread_repliesテーブルには一切触れないこと。
    worktree_needed: false

  - task: "Phase 0: wiki.py CLI骨格"
    bloom_level: 3
    recommended_model: claude-sonnet-4-6
    estimated_effort: "半日"
    description: |
      argparseでサブコマンド（show/stats/ingest/compile/lint/render/query/migrate）を定義。
      show/statsを先行実装。DB接続はbotsu/__init__.pyのget_connectionを流用。
    worktree_needed: false

skill_candidate: null
